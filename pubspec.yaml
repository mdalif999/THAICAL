name: thaical
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  intl: ^0.19.0
  supabase_flutter: ^2.8.0
  shared_preferences: ^2.2.0
  package_info_plus: ^8.0.0
  url_launcher: ^6.3.0
  permission_handler: ^11.3.1 # এখানে পারফেক্টলি সেট করে দেওয়া হয়েছে
  connectivity_plus: ^6.0.0
  uuid: ^4.4.0
  system_clock: ^2.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
