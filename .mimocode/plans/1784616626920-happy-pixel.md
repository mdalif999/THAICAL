# Fix: Hardware items merge kora (2+ janala hole alada alada dekhache)

## Samoshata
`calcHardwareBreakdown()` method `windowsList` er upor iterate kore, prithok window er jonyo item gulo **alada alada append** kore. Kono merging logic nai. Tai 2 ta janala hole "স্লাইডিং লক" 2 bar dekhabe.

## Fix Location
`lib/models/window_calculation.dart` — `calcHardwareBreakdown()` method (line ~230)

## Fix
For loop er pore, `return items` er age, merge logic add koro:
- Same name er items ke ekshathe merge koro
- `qty` add koro
- `cost` add koro
- `rate` same thakbe

```dart
// Loop er pore, return er age:
final merged = <String, Map<String, dynamic>>{};
for (var item in items) {
  final name = item['name'] as String;
  if (merged.containsKey(name)) {
    merged[name]!['qty'] = (merged[name]!['qty'] as int) + (item['qty'] as int);
    merged[name]!['cost'] = (merged[name]!['cost'] as double) + (item['cost'] as double);
  } else {
    merged[name] = Map<String, dynamic>.from(item);
  }
}
return merged.values.toList();
```

## Files to modify
- `lib/models/window_calculation.dart` — `calcHardwareBreakdown()` only

## Verification
- 2 windows add koro → step 3 e "স্লাইডিং লক" 1 bar dekhabe, qty dono window er jog
- 1 window add koro → same as before (no change)
- 3 windows add koro → qty tinobar jog
- Custom rate edit koro → merge er poreo kaj korbe
- WhatsApp copy + save invoice → merged qty/cost use hobe
